#!/usr/bin/env bash

selected=$(find ~/Music/local -type f | tr " " "\ " | fzf)
selected_name=$(basename "$selected")

tmux neww -n $selected_name bash -c "echo "Playing..."; afplay $selected; sleep 1"
